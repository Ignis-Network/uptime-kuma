version: '3.8'

services:
  uptime-kuma:
    image: louislam/uptime-kuma:1
    container_name: uptime-kuma
    volumes:
      - uptime-kuma:/app/data
    ports:
      - "8085:8085"  # <Host Port>:<Container Port>
    restart: always

volumes:
  uptime-kuma:

